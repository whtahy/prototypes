1  Initialize (draw board, start game, etc)
2  Game loop:
   a  Await input
   b  Update gamestate
   c  Update display

Custom keybinds
External img config
External BLT config
Save/load
Game record/history, a la Chess

ui -> game state -> render -> ui
